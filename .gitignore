build/
*.ipr
run/
*.iws
out/
*.iml
.gradle/
output/
bin/
libs/

.classpath
.project
.idea/
classes/
.metadata
.vscode
.settings
*.launch
# local deploy script (holds FTP credentials)
deploy.sh

# web frontend: node tooling and the build output are ignored (Gradle runs the Vite build itself — see :common:buildWebDist)
web/node_modules/
web/.vite/
web/dist/

# scratch + tooling leftovers
tmp/
.vite/
.architectury-transformer/
.claude/worktrees/
CLAUDE.md
